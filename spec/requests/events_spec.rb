require 'spec_helper'

describe "Events" do
  # describe "GET /events" do
  #   it "works! (now write some real specs)" do
  #     # Run the generator again with the --webrat flag if you want to use webrat methods/matchers
  #     get events_path
  #     response.status.should be(200)
  #   end
  # end

  # describe "add events" do

  # 	it "adds a new event and goes back to the events page" do
  # 		visit "/events/new"
  # 		fill_in "name", :with => "eventname"
  # 		fill_in "location", :with => "eventloc"
  # 		fill_in "date", :with => DateTime.new(params[:date][:year].to_i,params[:date][:month].to_i,params[:date][:day].to_i,params[:date][:hour].to_i,params[:date][:minute].to_i)
  # 		fill_in "description", :with => "eventdesc"
  # 		fill_in "link", :with => "eventlink"
  # 		click_button "Create Event"
  # 	end

  # end

end
