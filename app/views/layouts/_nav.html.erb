    <div class="nav-section">
      <div class="container">
      <div class="w-row">
        <div class="w-col w-col-3">
          <div class="company"><a href="/" style="text-decoration: none;color:white">
                        Recruit Berkeley</a>
          </div>
        </div>

        <% if current_user.recruiter %>
        <div class="w-col w-col-8 navigation-column"><%= link_to current_user.organizations.first.name, current_user.organizations.first, class:"nav-link" %> <%= link_to current_user.username, current_user, class:"nav-link" %>  <%= link_to "Logout", logout_path, class:"nav-link" %>

          <% else %>
        <div class="w-col w-col-8 navigation-column">

      

          <a class="nav-link" href="/home">Events</a><a class="nav-link" href="/jobs">Jobs</a><%= link_to current_user.username, current_user, class:"nav-link" %>  <%= link_to "Logout", logout_path, class:"nav-link" %>
          <div class="navbar-form navbar-right">
          <%=form_tag search_index_path, :method => 'get' do %>
          <%= text_field_tag :search, params[:search], class:'form-control search',placeholder:'Search for jobs/users' %>
          <%= submit_tag "Search", :name => nil, class:'w-button' %>

        <% end %>
      </div>
        <% end %>
        </div>
        
      </div>
    </div>
  </div>